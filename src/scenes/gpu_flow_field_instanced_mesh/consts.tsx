export const voxelsPerAxis = 32;
export const voxelSize = 1;
export const numVoxels = voxelsPerAxis ** 3;
export const textureWidth = Math.ceil(Math.sqrt(numVoxels));
export const textureHeight = textureWidth;
